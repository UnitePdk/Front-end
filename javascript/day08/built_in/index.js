const x=242.123;

Math.round(x); // 반올림
Math.floor(x); // 내림
Math.ceil(x); // 올림
Math.trunc(x); // 소수부분 제거

Math.abs(x) // 절댓값
Math.max(a,b,c) // 제일 큰 숫자 반환
Math.min(a,b,c) // 제일 작은 숫자 반환
Math.pow(x,y) // x의 y제곱 ( x ** y )